# Backend environment variables (copy to .env locally and fill real values)
DB_URL=jdbc:postgresql://localhost:5432/taskbridge
DB_USERNAME=postgres
DB_PASSWORD=

# Must be at least 32 characters for HS256
JWT_SECRET=replace-with-strong-32-plus-char-secret

MAIL_USERNAME=
MAIL_PASSWORD=
